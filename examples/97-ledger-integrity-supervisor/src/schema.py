"""Typed schemas for the ledger-integrity period-close supervisor.

TODO: ControlsExceptionRegister, plus typed exception shapes reused/adapted
from ids 44 (JV posting), 46 (bank reconciliation), 50 (fixed assets), and
55 (fraud/SAR), plus the new SoD (id 98) and audit-trail (id 99) findings.
"""
