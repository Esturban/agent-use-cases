"""LangGraph supervisor for the security operations pattern.

TODO: conditional dispatch across 5 domain subagents -> synthesise
SecurityPostureBrief -> route P0/P1 findings through the approval-gate
pattern (id 90) before treating a recommendation as actionable.
"""
